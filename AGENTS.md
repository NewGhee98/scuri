<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Scuri onboarding

Start with [START_HERE.md](START_HERE.md), the canonical current-status summary. Follow its reading order: CURRENT_TASK plus Git status, PROJECT_CONTEXT, VERIFICATION and the release checklist, then README and history as needed. Reconcile active notes with the current user request and preserve unrelated working changes.

Repository documentation owns technical handoff; Drive summaries link back to it. Recheck live release/configuration evidence before relying on dated observations. Missing image bytes never authorize deletion; documentation and historical procedures do not authorize deployments, migrations or live recovery. Keep the framework-managed block above intact.
